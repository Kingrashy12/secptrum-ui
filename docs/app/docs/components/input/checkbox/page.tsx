import { CheckboxDocs } from '@/components';
import { Metadata } from 'next';
import React from 'react';

export const metadata: Metadata = {
  title: 'Checkbox - Secptrum UI',
};

const Checkbox = () => {
  return (
    <>
      <CheckboxDocs />
    </>
  );
};

export default Checkbox;
