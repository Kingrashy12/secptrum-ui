import { ThemePage } from "@/components";
import React from "react";

export const metadata = {
  title: "Theme |Secptrum UI",
};

const Page = () => {
  return <ThemePage />;
};

export default Page;
